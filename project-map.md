# project-map

## 架构

完全在浏览器本地运行的视频/图片离线工具，无后端、无上传，文件经 Blob URL 解码与处理。多入口 SPA：6 个静态 HTML 入口（`/`、`/video-frame/`、`/video-compress/`、`/image-compress/`、`/image-watermark/`、`/image-a4-layout/`）各自携带唯一的 SEO meta 与 JSON-LD，但都加载同一个 `src/main.tsx`。`src/OfflineToolsApp.tsx` 按 `window.location.pathname` 选择渲染 `ToolHome`（`src/ToolHome.tsx`）、`FrameExtractor`（`src/App.tsx`）、`VideoCompressor`（`src/components/video-compressor.tsx`）、`ImageProcessor`（`src/tools/image-processor/ImageProcessor.tsx`）或 `A4ImageLayout`（`src/tools/image-a4-layout/A4ImageLayout.tsx`）。顶部导航（`ToolHeader`）为一级分组「视频/图片」（`TOOL_GROUPS`，点分组跳到组内默认工具），各工具页用 `ToolTabs`（shadcn Tabs + asChild 链接）渲染组内二级 tab；错误/完成反馈统一走 sonner，风险操作（清空/重新选择）用 `Popconfirm`（Radix Popover）二次确认。图片类三个入口（压缩/水印/A4 排版）在 `OfflineToolsApp` 中保持常挂载、按路由用 `hidden` 属性显隐，因此互切不丢已导入图片。`FrameExtractor` 支持单帧导出与批量抽帧：批量模式由 `src/lib/frame-sampling.ts` 的 `sampleFramesByInterval`/`sampleFramesByStep`/`sampleFramesEvenly` 按时间间隔、帧数步长或均匀取张算时间点，逐帧 seek + canvas 编码后由 `src/lib/zip-packer.ts` 的 `createFrameZipArchive`（jszip）流式打包为 ZIP（串行解码，`BATCH_DECODE_CONCURRENCY = 1`）。`VideoCompressor` 通过 `MediaRecorder` 把 `<video>` + `canvas.captureStream()` 重新编码为 WebM：`src/lib/video-export-scale.ts` 的 `computeScaledDimensions` 做等比缩放、`src/lib/video-export-config.ts` 的 `BITRATE_PRESETS`/`chooseBitrate`/`pickRecorderMime`（vp9+opus 优先，vp8+opus 降级）控码率与编码选择，音频轨取自 `video.captureStream()`。`A4ImageLayout` 排版时用 `src/lib/a4-layout.ts` 的 `COLS_PER_ROW`/`paginateFlow`（纯函数）按每行张数切行并按行高累加分页，页面（210mm × 297mm）屏幕预览用 `transform: scale` 缩放，打印走 `window.print()` + `@media print`，`@page { size: A4; margin: 0 }` 且页边距由页面内 padding 实现。站点对外绝对 URL 由 `src/lib/site.ts` 单一来源定义（`SITE_URL`/`OG_IMAGE_URL`/占位符 `SITE_URL_TOKEN`），换域名只需改该文件（或在构建环境提供 `VITE_SITE_URL` 覆盖）。六个入口 HTML 与 `public/sitemap.xml`、`public/robots.txt` 用 `__SITE_URL__` 占位符标记插入点，由 `vite.config.ts` 的 `offline-tools:site-url` 插件在构建期注入：`transformIndexHtml` 负责 HTML 入口，`closeBundle` 负责 `public/` 静态资源并兜底，且位于 `VitePWA(...)` 之前以保证预缓存内容修订号与实际产物一致；`tests/site-url-embed.test.mjs` 断言产物 host 唯一、无废弃 host、无残留占位符与修订号一致性。PWA 由 `vite-plugin-pwa` 预缓存，`registerType: "prompt"`，`navigateFallback: "/index.html"`（`/legal/` 在 denylist）。测试文件与源码同目录（`*.test.ts(x)`）加顶层 `tests/`（`.test.mjs` 构建产物校验）。

## 选型

Vite 8 + React 19 + TypeScript（`tsconfig.json`/`tsconfig.app.json`/`tsconfig.node.json`，路径别名 `@` → `src`）。Tailwind CSS 4（`@tailwindcss/vite`）+ shadcn/ui（new-york 风格，`components.json`，Radix primitives）。图标 lucide-react，toast sonner。PNG 压缩用 libimagequant-wasm + @jsquash/oxipng（WASM），Worker 由 esbuild 单独打包。视频压缩导出用浏览器原生 `MediaRecorder` + `canvas.captureStream`/`video.captureStream`（无额外 npm 依赖）。包管理 pnpm（`pnpm@10.15.0`），Node `>=22.13.0`。

## 路由

无 Hash Route、无 SPA rewrite 依赖（每条路由有真实 HTML 文件）。客户端导航在 `src/lib/tool-navigation.ts`：`TOOL_PATHS` 定义 6 条路径，`readToolRoute()` 按 pathname 后缀判定当前路由，`navigateToTool()` 用 `history.pushState` + 派发 `popstate` 做无刷新切换（`OfflineToolsApp` 监听 `popstate` 重渲染）。Vite 构建入口在 `vite.config.ts` 的 `build.rollupOptions.input`（home/videoFrame/videoCompress/imageCompress/imageWatermark/imageA4Layout）。压缩↔水印↔A4 排版间无刷新切换可保留已导入图片状态（图片工具分支常挂载 + `hidden` 显隐）。`src/lib/seo.ts` 的 `SEO_PAGES` + `useRouteSeo()` 在路由切换时更新 title/description/canonical/OG/JSON-LD。

## 公共组件

`src/components/`：`tool-header`（顶部导航，一级分组）、`tool-tabs`（二级工具 tab，shadcn Tabs）、`tool-links`（相关工具链接，全量 5 工具排除当前页）、`license-footer`（统一许可页脚，GPL 声明 + `/legal/` 链接）、`tool-seo-content`（各工具页 SEO 文案）、`icp-footer`（固定底部 ICP 备案链接，独立）、`pwa-status`（新版本提示）、`logo-mark`、`text-overlay-panel`（视频取帧文字叠加控制）、`batch-sampling-controls`（批量抽帧模式选择）、`video-compressor`（视频压缩导出，MediaRecorder 闭环）。`src/components/ui/`（shadcn 生成）：`button`、`card`、`select`、`slider`、`tabs`、`popconfirm`（Radix Popover）、`sonner`。`src/lib/theme.tsx` 提供明暗主题 `ThemeProvider`。

## 图片压缩管线

PNG 压缩在独立 Web Worker `src/workers/image-compress-worker.ts` 中执行，由 `src/lib/png-compressor.ts` 的 `PngCompressor` 通过 `postMessage` 调度（Worker 加载自 `${BASE_URL}assets/image-compress-worker.js`）。管线：libimagequant 解码 PNG→RGBA → `src/lib/png-strategy.ts` 的 `analyzePngPixels`/`choosePngCompressionPlan` 决定 `lossless` 或 `quantized` → OxiPNG 无损优化，必要时 libimagequant 量化调色板后再 OxiPNG 优化，取最小者；策略标签 `original`/`oxipng`/`libimagequant-oxipng`。JPG/WebP 走 Canvas `toBlob`，且不生成比原图更大的文件（`ImageProcessor.compressAsset`）。Worker 由 `scripts/build-image-codecs.mjs` 用 esbuild 预构建到 `public/assets/image-compress-worker.js`（含 GPL 法律 banner，可复现构建见 `CORRESPONDING_SOURCE.md`），该产物已 gitignore；`pnpm dev`/`pnpm build` 前置自动运行 `build:image-codecs`。

## 图片 A4 排版打印

`A4ImageLayout`（`src/tools/image-a4-layout/A4ImageLayout.tsx`）：以本地 Blob URL 导入多张图片（拖放或文件选择，导入时解码一次取宽高比，解码失败跳过并提示「N 张图片无法解码」），列表支持上移/下移/移除/清空（两步确认）。排版计算全部在 `src/lib/a4-layout.ts` 纯函数中：`COLS_PER_ROW`（每行 1–6 张，默认 3）、`MARGIN_MM`（5–25mm，默认 10mm）、`paginateFlow`（按每行张数切行、行高取该行最高图并 0.01mm 取整、按行累加分页，末页可不满、无空白页）、`moveItem`（排序）。页面单 DOM：`.a4-sheet` 为 210mm × 297mm 的纵向行容器（`.a4-flow-row` 行高 `--a4-row-h` 由布局计算注入，`.a4-flow-cell` 内图片 `max-width/max-height: 100%` + `object-fit: contain` 不裁切），页边距是页面内 padding；屏幕端只显示当前页（`.a4-sheet-hidden` 隐藏其余页），预览容器 `ResizeObserver` 计算 `transform: scale` 缩放并居中定位。打印走 `window.print()` + `src/index.css` 的 `@media print`：`@page { size: A4; margin: 0 }`、UI 元素全部隐藏、非末页 `break-after: page`、隐藏页恢复 `display: flex` 全输出、行辅助线关闭。与压缩/水印的无刷新切换由 `OfflineToolsApp` 常挂载保证。
